<h1>Listing owners</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Facebookappid</th>
      <th>Description</th>
      <th>Facebookappkey</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @owners.each do |owner| %>
      <tr>
        <td><%= User.find(owner.User_id).name %></td>
        <td><%= owner.facebookappid %></td>
        <td><%= owner.description %></td>
        <td><%= owner.facebookappkey %></td>
        <td><%= link_to 'Show', owner %></td>
        <td><%= link_to 'Edit', edit_owner_path(owner) %></td>
        <td><%= link_to 'Destroy', owner, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Owner', new_owner_path %>
